# Leave empty in development — Vite proxies /api → http://localhost:8000.
# In production set to the absolute backend origin (no trailing slash) if you
# are NOT using Vercel rewrites, e.g.:
#   VITE_API_BASE_URL=https://veritas-api.onrender.com
VITE_API_BASE_URL=
